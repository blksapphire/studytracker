package com.mayowa.studytracker.presentation.theme

import androidx.compose.ui.graphics.Color

// ---- Core brand: teal (focus, calm, "growth") ----
val TealPrimary = Color(0xFF0FB8A6)       // main brand color — CTAs, progress rings, nav selection
val TealPrimaryDark = Color(0xFF0A8577)   // pressed/dark-mode primary
val TealPrimaryContainer = Color(0xFFB2F2E8)
val TealOnPrimaryContainer = Color(0xFF00352E)

// ---- Gamification accents: bright, high-energy, used sparingly ----
val StreakCoral = Color(0xFFFF6B4A)       // streak flame, urgency, "don't break the chain"
val StreakCoralContainer = Color(0xFFFFDBD1)
val XpAmber = Color(0xFFFFC93C)           // badges, milestones, level-up moments
val XpAmberContainer = Color(0xFFFFF0C2)

// ---- Neutrals: warm-cool grays, not stark black/white — keeps "playful" from tipping into "childish" ----
val SurfaceLight = Color(0xFFF7FAF9)
val SurfaceDark = Color(0xFF0E1615)
val OnSurfaceLight = Color(0xFF161D1C)
val OnSurfaceDark = Color(0xFFDEE4E2)

// ---- Semantic ----
val ErrorRed = Color(0xFFE5484D)
val SuccessGreen = Color(0xFF2FB170) // distinct from primary teal — used for "goal hit" states only
